extends layout

block content
  h1= title
  ul
    each link in links
      li
        a(href=link.href)= link.name
        |  
        if link.slideShowUrl
          a.ss-enter-button(href=link.slideShowUrl) Slideshow
